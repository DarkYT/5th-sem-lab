#include <stdio.h>
#include <string.h>
int main()
{
    char arr1[20];
    int count = 0;
    printf("\n Enter a string 1 :");
    gets(arr1);
    char arr2[20];
    int len = 0;
    printf("\n Enter a string 2 :");
    gets(arr2);
    int len1 = strlen(arr1);
    int len2 = strlen(arr2);
    int len3=len1+len2;
    char arr3[len3];
    for(int i=0;i<len3;){
        arr3[i]=arr1[i]
  
    }
}
